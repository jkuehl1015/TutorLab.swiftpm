import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("Welcome To TutorLab")
                    .font(.largeTitle)
                    .fontWeight(.black)
            }
        }
    }
}
